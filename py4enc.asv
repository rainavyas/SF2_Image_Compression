function py4enc(X)

%Generate a 4-layer pyramid, so that X is split into Y0, Y1, Y2, Y3 and X4

X_zero_mean = X-128;

X1 = quarter_size_lowpass(X_zero_mean);
Y0 = highpass_from_decimated_lowpass(X1)




